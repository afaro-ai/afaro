# A file with three things in it to find

The first is a bare word standing on its own, and here it is: Quill.

The second is written with two spaces in the middle, Marisol  Quillfeather,
which the sweep should still see as one value.

The third is a number written with dots rather than dashes, 555.0142, which
only the digits-only pass catches.
